import { Util } from 'ibiz-core';
import { Vue, Component, Prop} from 'vue-property-decorator';

@Component({})
export class AppDataViewItem extends Vue {
    public render() {
        return <div></div>
    }
}