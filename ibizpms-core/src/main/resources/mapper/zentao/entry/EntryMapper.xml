<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper	namespace="cn.ibizlab.pms.core.zentao.mapper.EntryMapper">
	
	<!-- 无视图模式 -->
	<sql id="select" databaseId="mysql">
        <![CDATA[ SELECT t1.`ACCOUNT`, t1.`CALLEDTIME`, t1.`CODE`, t1.`CREATEDBY`, t1.`CREATEDDATE`, t1.`DELETED`, t1.`DESC`, t1.`EDITEDBY`, t1.`EDITEDDATE`, t1.`FREEPASSWD`, t1.`ID`, t1.`IP`, t1.`KEY`, t1.`NAME` FROM `zt_entry` t1   ]]>
    </sql>

	
	<!-- 搜索条件 -->
	<sql id="searchMode" databaseId="mysql">
		<if test="srf.n_freepasswd_eq != null ">
			<![CDATA[ AND  t1.`FREEPASSWD` = #{srf.n_freepasswd_eq,jdbcType=VARCHAR} ]]>
        </if>
		<if test="srf.n_name_like != null ">
            <bind name="bind_n_name_like" value="'%' + srf.n_name_like + '%'" />
            <![CDATA[ AND  t1.`NAME` like #{bind_n_name_like,jdbcType=VARCHAR} ]]>
        </if>
		<if test="ew!=null and ew.sqlSegment!=null and ew.sqlSegment!=''">
		AND ${ew.sqlSegment}
		</if>
	</sql>


    <!--该方法用于重写mybatis中selectById方法，以实现查询逻辑属性-->
	<select id="selectById"  resultMap="EntryResultMap" databaseId="mysql">
        <include refid="select" />
		<trim prefix="where" prefixOverrides="and">
			<if test="true">
			<![CDATA[ AND t1.DELETED = '0' ]]>
			</if>
			<![CDATA[ AND T1.ID = #{id} ]]>
		</trim>
    </select>


    <!--通过mybatis将查询结果注入到entity中,通过配置autoMapping="true"由mybatis自动处理映射关系 -->
    <resultMap id="EntryResultMap" type="cn.ibizlab.pms.core.zentao.domain.Entry" autoMapping="true">
		<id property="id" column="id" /><!--主键字段映射-->
            
		
    </resultMap>


    <!--数据集合[Default]-->
	 <select id="searchDefault"  parameterType="cn.ibizlab.pms.core.zentao.filter.EntrySearchContext"  resultMap="EntryResultMap">
         select t1.* from (
                <include refid="Default" />
         )t1
     </select>

    <!--数据查询[Default]-->
    <sql id="Default" databaseId="mysql">
		<![CDATA[ SELECT t1.`ACCOUNT`, t1.`CALLEDTIME`, t1.`CODE`, t1.`CREATEDBY`, t1.`CREATEDDATE`, t1.`DELETED`, t1.`EDITEDBY`, t1.`EDITEDDATE`, t1.`FREEPASSWD`, t1.`ID`, t1.`IP`, t1.`KEY`, t1.`NAME` FROM `zt_entry` t1   ]]>
		<trim prefix="where" prefixOverrides="and">
			<if test="true">
			<![CDATA[ AND t1.DELETED = '0' ]]>
			</if>
			<include refid="searchMode" />
		</trim>
    </sql>
	
	<select id="selectDefault"  parameterType="cn.ibizlab.pms.core.zentao.filter.EntrySearchContext" resultMap="EntryResultMap">
		<include refid="Default" />
	</select>

    <!--数据查询[View]-->
    <sql id="View" databaseId="mysql">
		<![CDATA[ SELECT t1.`ACCOUNT`, t1.`CALLEDTIME`, t1.`CODE`, t1.`CREATEDBY`, t1.`CREATEDDATE`, t1.`DELETED`, t1.`DESC`, t1.`EDITEDBY`, t1.`EDITEDDATE`, t1.`FREEPASSWD`, t1.`ID`, t1.`IP`, t1.`KEY`, t1.`NAME` FROM `zt_entry` t1   ]]>
		<trim prefix="where" prefixOverrides="and">
			<if test="true">
			<![CDATA[ AND t1.DELETED = '0' ]]>
			</if>
			<include refid="searchMode" />
		</trim>
    </sql>
	
	<select id="selectView"  parameterType="cn.ibizlab.pms.core.zentao.filter.EntrySearchContext" resultMap="EntryResultMap">
		<include refid="View" />
	</select>

</mapper>

