import { FormDetailModel } from './form-detail';

/**
 * 直接内容模型
 *
 * @export
 * @class FormRawItemModel
 * @extends {FormDetailModel}
 */
export class FormRawItemModel extends FormDetailModel {


    constructor(opts: any = {}) {
        super(opts);
    }
}